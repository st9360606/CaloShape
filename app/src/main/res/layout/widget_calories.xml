<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_calories_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <FrameLayout
        android:id="@+id/widget_calories_card"
        android:layout_width="170dp"
        android:layout_height="175dp"
        android:layout_gravity="center"
        android:background="@drawable/widget_card_bg"
        android:paddingStart="6dp"
        android:paddingTop="8dp"
        android:paddingEnd="6dp"
        android:paddingBottom="8dp">

        <FrameLayout
            android:id="@+id/widget_calories_ring_area"
            android:layout_width="112dp"
            android:layout_height="112dp"
            android:layout_gravity="top|center_horizontal"
            android:layout_marginTop="4dp">

            <ImageView
                android:id="@+id/widget_calories_ring"
                android:layout_width="112dp"
                android:layout_height="112dp"
                android:src="@drawable/widget_preview_calories_ring"
                android:contentDescription="@null" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingStart="6dp"
                android:paddingEnd="6dp">

                <TextView
                    android:id="@+id/widget_calories_value"
                    android:layout_width="64dp"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:fontFamily="sans-serif"
                    android:gravity="center"
                    android:textStyle="bold"
                    android:includeFontPadding="false"
                    android:maxLines="1"
                    android:text="@string/widget_preview_goal_kcal"
                    android:textColor="#FF111114"
                    android:textSize="26sp" />

                <TextView
                    android:id="@+id/widget_calories_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="3dp"
                    android:ellipsize="end"
                    android:fontFamily="sans-serif-medium"
                    android:includeFontPadding="false"
                    android:maxLines="1"
                    android:text="@string/widget_calories_left"
                    android:textColor="#FF8D9198"
                    android:textSize="11sp" />
            </LinearLayout>
        </FrameLayout>

        <FrameLayout
            android:id="@+id/widget_calories_action"
            android:layout_width="145dp"
            android:layout_height="36dp"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="0dp"
            android:translationY="0dp"
            android:background="@drawable/widget_action_dark_bg"
            android:paddingStart="6dp"
            android:paddingEnd="9dp">

            <FrameLayout
                android:id="@+id/widget_calories_plus"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="start|center_vertical"
                android:background="@drawable/widget_circle_white_bg">

                <ImageView
                    android:id="@+id/widget_calories_plus_icon"
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="center"
                    android:contentDescription="@null"
                    android:src="@drawable/ic_widget_plus_black" />
            </FrameLayout>

            <TextView
                android:id="@+id/widget_calories_action_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginStart="32dp"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:text="@string/widget_log_your_food"
                android:textColor="#FFFFFFFF"
                android:textSize="15sp" />
        </FrameLayout>
    </FrameLayout>
</FrameLayout>
